#include <stdio.h>

int main() {
    int arr[] = {10, 15, 20, 25, 30};
    int n = sizeof(arr) / sizeof(arr[0]);

    for (int i = 0; i < n; i++)
        printf("%d is %s\n", arr[i], arr[i] % 2 == 0 ? "even" : "odd");

    return 0;
}
